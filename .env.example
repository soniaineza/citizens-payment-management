# Copy this file to .env and adjust the values.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your-postgres-password
DB_NAME=citizen_register

# Use a long random string here (e.g. openssl rand -hex 32)
JWT_SECRET=change-me-to-a-long-random-string
PORT=5000